<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".VolumeBridgeApp"
        android:allowBackup="true"
        android:icon="@android:drawable/ic_lock_silent_mode"
        android:label="@string/app_name"
        android:theme="@style/Theme.VolumeBridge">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL"/>
        </activity>

        <!--
             MacroDroid target: "Send Intent" action, type Broadcast,
             Action = com.volumebridge.app.SET_VOLUME
             Extra "package" (String)  = z.B. com.google.android.youtube
             Extra "volume"  (Float)   = 0.0 bis 1.0
        -->
        <receiver
            android:name=".VolumeBridgeReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.volumebridge.app.SET_VOLUME" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
